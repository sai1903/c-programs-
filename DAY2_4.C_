#include<stdio.h>
#include<conio.h>
void main()
{
 int a[50],b[50],i,n;
 printf("\nenter no of elements");
 scanf("%d",&n);
 printf("\n enter 1st array elements");
 for(i=0;i<n;i++)
 {
  scanf("%d\t",&a[i]);
  }
  printf("\nenter 2nd array elements");
  for(i=0;i<n;i++);
  {
  scanf("%d",&b[i]);
  }
  printf("copying");
  for(i=0;i<n;i++)
  {
   b[i]=a[i];
   }
   printf("\narray after copying ");
   for(i=0;i<n;i++)
   printf("b[%d]=%d",i,b[i]);
  getch();
 }
